import { CurrentProjectsComponent } from './current-projects.component';

describe('CurrentProjectsComponent', () => {
  it('should create', () => {
    const component = new CurrentProjectsComponent();
    expect(component).toBeTruthy();
  });
});