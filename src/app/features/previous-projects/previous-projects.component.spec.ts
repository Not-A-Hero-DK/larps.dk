import { PreviousProjectsComponent } from './previous-projects.component';

describe('PreviousProjectsComponent', () => {
  it('should create', () => {
    const component = new PreviousProjectsComponent();
    expect(component).toBeTruthy();
  });
});